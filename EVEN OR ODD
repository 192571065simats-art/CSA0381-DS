#include <stdio.h>

int main() {
    int a[] = {10, 7, 4, 9, 12};
    int n = 5, i;

    for(i = 0; i < n; i++) {
        if(a[i] % 2 == 0)
            printf("%d is Even\n", a[i]);
        else
            printf("%d is Odd\n", a[i]);
    }

    return 0;
}
