#include <stdio.h>

int main()
{
    int n = 5, i;
    long long factorial = 1;

    for(i = 1; i <= n; i++)
    {
        factorial = factorial * i;
    }

    printf("Factorial of %d = %lld\n", n, factorial);

    return 0;
}
